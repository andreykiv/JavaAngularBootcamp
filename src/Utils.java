
public class Utils {
	/*
	 * ArrayList<String> myArrayLikeList = new ArrayList<>();
	 * 
	 * ArrayList<Double> myArrayLikeListDouble = new ArrayList<>();
	 * myArrayLikeListDouble.add(2.22); myArrayLikeListDouble.add(9.00);
	 * myArrayLikeListDouble.add(1.222);
	 * 
	 * Hashtable<String, String> contenedor = new Hashtable<String, String>();
	 * 
	 * contenedor.put("case1", "spiderman"); contenedor.put("case2", "ironman");
	 * 
	 * Enumeration<String> enumeration = contenedor.elements();
	 * System.out.println(contenedor.get("case1"));
	 * if(enumeration.hasMoreElements()) {
	 * System.out.println(enumeration.nextElement()); }
	 */
}
